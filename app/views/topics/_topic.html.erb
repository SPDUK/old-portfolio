<div class="mt-1">
  <%= link_to "Delete", topic_path(topic), method: :delete, class: "mr-3 btn btn-sm btn-danger", data: {confirm: "Are you sure you want to delete #{topic.title}?"} if logged_in?(:site_admin) %>
  <%= link_to topic.title, topic_path(topic) %>
</div>
